process {
  resourceLimits = [
      memory: 128.GB,
      cpus: 256,
      time: 7.d,
  ]

  errorStrategy = { task.exitStatus in [140,137] ? 'retry' : 'finish' }
  maxRetries = 1
  maxErrors = '-1'
  
  cpus = 4
}
